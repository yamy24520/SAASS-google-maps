document.querySelector('#start').onclick=async()=>{try{const [tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab.url?.startsWith('https://www.google.com/maps/'))throw new Error('Ouvrez une fiche sur www.google.com/maps.');await chrome.scripting.executeScript({target:{tabId:tab.id},files:['collector.js']});window.close()}catch(e){document.querySelector('#message').textContent=e.message}};
